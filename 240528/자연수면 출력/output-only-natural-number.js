const fs = require ("fs");

let input = fs.readFileSync(0).toString().split(" ");

a = input[0];
b = Number(input[1]);
let result = []
if {(typeof a === Number) {
    for (let i = 1; i<=b; i++){
    result += a +""
    }
}
    console.log(result);
}
else if ( a<=0) {
    console.log(0);
}